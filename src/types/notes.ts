interface BoardType {
    boardId: number;
    boardName: string;
}

interface ClassType {
    classId: number;
    className: string;
}

interface SubjectType {
    subjectId: number;
    subjectName: string;
}

interface ChapterType {
    chapterId: number;
    chapterName: string;
}


export type {BoardType , ClassType, SubjectType, ChapterType};
